const nameTwo=new String('Ryu');
//it creates a new wrapper object
//JS wraps primitive value in an object
new Number(5);
new Boolean();
//only types that does not have a porimitive value are null and undefined
//Though everything in JS is not an object everything in JS can behave like an object